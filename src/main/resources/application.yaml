server:
  port: 8889

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/asset_management
    username: postgres
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true